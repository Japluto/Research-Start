好的，这是一个非常棒的计划！使用《C++ Primer Plus》这样的经典教材来配合 PPT 进行巩固，是最高效的学习方法。

我已经帮你把你**完整五天**的速通计划，与你提供的《C++ Primer Plus (第6版) 中文版》的目录**精确匹配**。

**学习建议**：你不需要**完整阅读**这几章。C++ Primer Plus 非常详细，为了“速通”，你应该把它当作“**高精度字典**”来用。只**精读**我为你列出的小节，这些是与你的PPT考点**完全对应**的。

-----

### C++ 5日速通（教材巩固版）

#### Day 1: C++基础 & 类的入门 (主攻 PPT 1, 2, 8)

  * **目标：** 脱离C，开始用C++的方式思考和写代码。理解“类”是C++的灵魂。

| 模块/PPT主题 | C++ Primer Plus 对应章节 (第6版) | 复习重点 |
| :--- | :--- | :--- |
| **`iostream` / `namespace`** | **第2章 开始学习C++** (p. 12) <br> \* `2.1.3 C++预处理器和 iostream 文件` (p. 16) <br> \* `2.1.5 名称空间` (p. 17) <br> \* `2.1.6 使用 cout 进行C++输出` (p. 18) <br> \* `2.3.1 使用cin` (p. 24) | 快速浏览，搞懂 `iostream`、`cin`、`cout` 和 `using namespace std;` 的用法。 |
| **`const` 替代 `#define`** | **第3章 处理数据** (p. 37) <br> \* `3.2 const 限定符` (p. 54) <br> **附录 I 转换为ISO标准C++** (p. 911) <br> \* `I.1.1 使用 const 而不是#define ...` (p. 911) | 重点理解 `const` 相比 `#define` 的优势（有类型检查）。 |
| **`new` / `delete`** | **第4章 复合类型** (p. 70) <br> \* `4.7.4 使用 new 来分配内存` (p. 102) <br> \* `4.7.5 使用 delete 释放内存` (p. 104) <br> \* `4.7.6 使用 new 来创建动态数组` (p. 104) | 掌握 `new`/`delete` 和 `new []`/`delete []` 的配对使用。这是C++内存管理的基础。 |
| **引用 (Reference `&`)** | **第8章 函数探幽** (p. 253) <br> \* `8.2 引用变量` (p. 255) <br> \* `8.2.1 创建引用变量` (p. 256) <br> \* `8.2.2 将引用用作函数参数` (p. 258) | **(C++重点)** 这是C语言没有的。必须搞懂 "别名" 的概念，尤其是 `void swap(int &a, int &b)` 如何替代指针。 |
| **函数重载 (Overload)** | **第8章 函数探幽** (p. 253) <br> \* `8.4 函数重载` (p. 276) <br> \* `8.4.1 重载示例` (p. 278) | 理解C++如何通过**参数列表**（类型、个数、顺序）来区分同名函数。 |
| **`inline` 内联函数** | **第8章 函数探幽** (p. 253) <br> \* `8.1 C++内联函数` (p. 253) <br> **附录 I 转换为ISO标准C++** (p. 911) <br> \* `I.1.2 使用 inline 而不是#define ...` (p. 912) | 理解 `inline` 是C++用来替代带参宏 (`#define ADD(a,b)`) 的安全方式。 |
| **类的入门 (`class`, `struct`)** | **第10章 对象和类** (p. 340) <br> \* `10.2 抽象和类` (p. 341) <br> \* `10.2.2 C++中的类` (p. 342) | 搞懂 `class` 和 `struct` 的唯一区别（默认访问权限）。 |
| **封装 (`public`/`private`)** | **第10章 对象和类** (p. 340) <br> \* `10.2.2 C++中的类` (p. 342) | **(C++重点)** 理解 `public`（接口）和 `private`（实现）的含义。 |
| **接口与实现分离** | **第9章 内存模型和名称空间** (p. 300) <br> \* `9.1 单独编译` (p. 300) <br> **第10章 对象和类** (p. 340) <br> \* `10.2.3 实现类成员函数` (p. 345) | 学会 `::` 作用域解析运算符，知道如何在 `.h` 中声明类，在 `.cpp` 中实现成员函数。 |

-----

#### Day 2: 类的核心机制 (主攻 PPT 3)

  * **目标：** **（最重要的一天）** 掌握类的所有关键机制，尤其是构造函数、析构函数、拷贝构造、`static` 和 `const`。

| 模块/PPT主题 | C++ Primer Plus 对应章节 (第6版) | 复习重点 |
| :--- | :--- | :--- |
| **构造函数 (Constructor)** | **第10章 对象和类** (p. 340) <br> \* `10.3 类的构造函数和析构函数` (p. 352) <br> \* `10.3.1 声明和定义构造函数` (p. 353) <br> \* `10.3.3 默认构造函数` (p. 354) | 掌握构造函数与类名相同、无返回值的特性。 |
| **构造函数重载/默认参数** | **第10章 对象和类** (p. 340) <br> \* `10.3.5 改进 Stock 类` (p. 356) <br> **第8章 函数探幽** (p. 253) <br> \* `8.3 默认参数` (p. 274) | 重点看 `10.3.5` (p. 356) 的例子，理解如何用默认参数合并多个构造函数。 |
| **析构函数 (Destructor)** | **第10章 对象和类** (p. 340) <br> \* `10.3.4 析构函数` (p. 355) | 掌握 `~ClassName()` 语法，理解其调用时机（与构造相反，LIFO）。 |
| **`this` 指针** | **第10章 对象和类** (p. 340) <br> \* `10.4 this 指针` (p. 363) | 知道 `this` 是指向对象本身的指针。`this->member` 等价于 `member`。 |
| **拷贝构造函数 (Copy)** | **第12章 类和动态内存分配** (p. 425) <br> \* `12.1.2 特殊成员函数` (p. 432) <br> \* `12.1.3 ...复制构造函数...` (p. 434) | **(C++重点)** 必须背诵 `ClassName(const ClassName& obj)` 格式。理解“深拷贝”和“浅拷贝”的区别，以及为什么参数必须是引用。 |
| **`static` 静态成员** | **第12章 类和动态内存分配** (p. 425) <br> \* `12.1.1 复习示例和静态类成员` (p. 425) <br> \* `12.2.4 静态类成员函数` (p. 441) | 掌握2个考点：1. 静态数据成员**类外初始化**。2. 静态成员函数**只能访问**静态成员。 |
| **`const` 成员/对象** | **第10章 对象和类** (p. 340) <br> \* `10.6.1 作用域为类的常量` (p. 371) | **(C++重点)** 掌握3个 `const` 用法：<br>1. **常数据成员**：必须在构造函数初始化列表里初始化。<br>2. **常成员函数**：`void func() const;`，保证不修改成员。<br>3. **常对象**：`const MyClass obj;`，只能调用 `const` 成员函数。 |
| **友元 (`friend`)** | **第11章 使用类** (p. 380) <br> \* `11.3 友元` (p. 390) <br> \* `11.3.1 创建友元` (p. 391) <br> **第15章 友元、异常和其他** (p. 602) <br> \* `15.1.1 友元类` (p. 602) | 理解 `friend` 是为了“破坏封装”，允许外部函数或类访问 `private` 成员。 |

-----

#### Day 3: 运算符重载 & IO流 (主攻 PPT 4, 7)

  * **目标：** 掌握如何让你的“类”用起来像C++的内置类型（如 `int`）。掌握文件读写。

| 模块/PPT主题 | C++ Primer Plus 对应章节 (第6版) | 复习重点 |
| :--- | :--- | :--- |
| **运算符重载 (基础)** | **第11章 使用类** (p. 380) <br> \* `11.1 运算符重载` (p. 380) <br> \* `11.2 时间:一个运算符重载示例` (p. 381) <br> \* `11.2.2 重载限制` (p. 387) | 掌握 `operator+` 这样的函数名语法。记住哪些不能重载 (`.`, `::`, `?:` 等)。 |
| **成员 vs 友元重载** | **第11章 使用类** (p. 380) <br> \* `11.4 重载运算符:作为成员函数还是非成员函数` (p. 397) | **(C++重点)** 理解 `c1 + c2` 既可以被解释为 `c1.operator+(c2)` (成员) 也可以被解释为 `operator+(c1, c2)` (友元/非成员)。 |
| **重载 `<<` 和 `>>`** | **第11章 使用类** (p. 380) <br> \* `11.3.2 常用的友元:重载<<运算符` (p. 392) <br> **第17章 输入、输出和文件** (p. 731) <br> \* `17.2.1 重载的<<运算符` (p. 736) | **(必考)** 必须背诵 `ostream& operator<<(ostream& os, const MyClass& obj)` 签名。必须理解为何它**不能**是成员函数，以及为何要返回 `ostream&` (为了链式调用)。 |
| **C++ I/O 流 (格式化)** | **第17章 输入、输出和文件** (p. 731) <br> \* `17.2.4 用 cout 进行格式化` (p. 741) <br> \* `17.3.3 其他 istream 类方法` (p. 759) | 对应 PPT 7。复习 `get()`, `getline()`, `peek()`, `ignore()` 和 `setw()`, `setprecision()` 等格式化IO。 |
| **文件I/O (`fstream`)** | **第17章 输入、输出和文件** (p. 731) <br> \* `17.4 文件输入和输出` (p. 768) <br> \* `17.4.1 简单的文件I/O` (p. 768) <br> \* `17.4.5 文件模式` (p. 773) | 掌握 `ifstream` (读) 和 `ofstream` (写)。理解 `ios::app` (追加) 和 `ios::binary` (二进制) 模式。 |
| **二进制 I/O (`read`/`write`)** | **第17章 输入、输出和文件** (p. 731) <br> \* `17.4.6 随机存取` (p. 781) | 对应 PPT 7。重点看 `read((char*)&obj, sizeof(obj))` 这种二进制读写 `struct`/`class` 的语法。 |

-----

#### Day 4: 继承 (主攻 PPT 5)

  * **目标：** 理解面向对象的“复用”特性。掌握派生类的构造和访问规则。

| 模块/PPT主题 | C++ Primer Plus 对应章节 (第6版) | 复习重点 |
| :--- | :--- | :--- |
| **继承 (is-a 关系)** | **第13章 类继承** (p. 480) <br> \* `13.1.1 派生一个类` (p. 482) <br> \* `13.2 继承: is-a 关系` (p. 489) | 掌握 `class Derived : public Base` 语法。理解 "Student is-a Person"。 |
| **访问控制 (`protected`)** | **第13章 类继承** (p. 480) <br> \* `13.5 访问控制: protected` (p. 507) | **(C++重点)** `protected` 是专门为继承设计的：派生类可访问，但类外部不可访问。 |
| **继承方式 (public/private)** | **第14章 C++中的代码重用** (p. 534) <br> \* `14.2 私有继承` (p. 543) <br> \* `14.2.3 保护继承` (p. 549) | **(考点)** 熟记PPT 5 (Page 10) 的表格：基类的 `public`/`protected` 成员在 `public` 继承后不变；在 `private` 继承后都变 `private`。基类的 `private` 成员永远不可被派生类访问。 |
| **派生类构造/析构** | **第13章 类继承** (p. 480) <br> \* `13.1.2 构造函数:访问权限的考虑` (p. 484) | **(必考)** 必须掌握派生类构造函数**在初始化列表**中调用基类构造函数 `Derived(args) : Base(args) {}`。<br> **顺序：** 构造：基类 -\> 派生类。析构：派生类 -\> 基类。 |
| **多重继承** | **第14章 C++中的代码重用** (p. 534) <br> \* `14.3 多重继承` (p. 551) | 掌握 `class D : public A, public B` 语法。 |
| **二义性 (菱形继承)** | **第14章 C++中的代码重用** (p. 534) <br> \* `14.3.1 有多少 Worker...` (p. 555) | **(考点)** 理解“菱形继承” (`A` -\> `B`, `A` -\> `C`, `B,C` -\> `D`) 为何会导致 `D` 中有两份 `A`，从而产生二义性。 |
| **虚基类 (`virtual`)** | **第14章 C++中的代码重用** (p. 534) <br> \* `14.3.1 有多少 Worker...` (p. 555) | 掌握 `class B : virtual public A` 语法。理解 `virtual` 继承使 `D` 中只保留一份 `A`，解决了二义性。掌握 `D` 的构造函数必须**直接调用** `A` 的构造函数。 |

-----

#### Day 5: 多态 & 总复习 (主攻 PPT 6, 8)

  * **目标：** 掌握C++“面向对象”的精髓——`virtual` 虚函数。并进行模拟冲刺。

| 模块/PPT主题 | C++ Primer Plus 对应章节 (第6版) | 复习重点 |
| :--- | :--- | :--- |
| **静态/动态联编** | **第13章 类继承** (p. 480) <br> \* `13.4 静态联编和动态联编` (p. 501) | 理解**静态联编**（编译时）和**动态联编**（运行时）的区别。 |
| **虚函数 (`virtual`)** | **第13章 类继承** (p. 480) <br> \* `13.3 多态公有继承` (p. 490) <br> \* `13.4.2 虚成员函数和动态联编` (p. 503) | **(C++灵魂)** 核心！理解 `virtual` 如何启用动态联编。掌握基类指针 `Base* p = new Derived();`，当调用 `p->func()` 时，如果 `func` 是虚函数，则执行 `Derived` 的版本。 |
| **虚析构函数** | **第13章 类继承** (p. 480) <br> \* `13.7.2 第二种情况:派生类使用 new` (p. 517) | **(必考)** 只要一个类可能被继承，其析构函数**必须**声明为 `virtual ~MyClass()`。这是为了防止 `delete p;` (基类指针) 时只调用基类析构，导致派生类内存泄漏。 |
| **抽象基类 (ABC)** | **第13章 类继承** (p. 480) <br> \* `13.6 抽象基类` (p. 508) | 掌握**纯虚函数** `virtual void func() = 0;` 的语法。理解带纯虚函数的类是**抽象类**，不能被实例化。 |
| **编程规范 (总复习)** | **附录 I 转换为ISO标准C++** (p. 911) <br> \* `I.1.1 使用 const 而不是#define...` (p. 911) <br> \* `I.1.2 使用 inline 而不是#define...` (p. 912) <br> \* `I.5 使用新的头文件` (p. 914) <br> \* `I.6 使用名称空间` (p. 914) | 对应 PPT 8。复习为什么 `const`/`inline` 优于 `#define`，`iostream` 优于 `stdio.h`，`new`/`delete` 优于 `malloc`/`free`。 |